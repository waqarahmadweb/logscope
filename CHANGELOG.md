# Changelog

All notable changes to this project are documented here. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

-   `.wordpress-org/README.md` spec doc (Phase 17.2) — scaffolds the directory wp.org's SVN `assets/` will mirror at submission time. Documents the eight binary assets the v1.0.0 cut needs (banner 1544×500 PNG, icon 256×256 PNG, six 1280-px-wide screenshots) with captions sourced verbatim from `readme.txt`'s `== Screenshots ==` block so the two cannot drift, plus banner safe-area / icon padding / screenshot framing guidance for the maintainer producing them by hand. The directory is `export-ignore`d from the distribution zip; wp.org pulls these from SVN, not from the plugin zip itself.

-   `readme.txt` in wp.org plugin-directory format (Phase 17.1) — Contributors, Tags, Requires at least, Tested up to, Stable tag, Requires PHP, License, License URI, Description, Installation, FAQ, Changelog, Screenshots, Upgrade Notice, Privacy. Description mirrors the README.md feature list; the Changelog section condenses CHANGELOG.md 0.1.0 → 0.14.0 into wp.org-flavored bullets (one paragraph + 2–4 highlights per shipped version) so the `wordpress.org/plugins/logscope/` listing is self-contained without back-references to the GitHub CHANGELOG. `Tested up to: 6.9` resolves the latest stable WP at session time (6.9.4) to the major.minor wp.org accepts. Privacy section names the only outbound paths (email via `wp_mail()` and webhook POST, both off by default) and the diagnostics REST endpoint, so the wp.org reviewer's "what does this plugin send off-site" check has a one-paragraph answer.

## [0.14.0] - 2026-04-30

Closes Phase 16 of the [roadmap](ROADMAP.md): three features bundled into one release that close the gap between "Logscope is installed" and "Logscope is useful." A first-time admin opening the plugin on a host with `WP_DEBUG_LOG` off now sees an actionable onboarding banner with the exact lines to drop into `wp-config.php` and a link to the WordPress handbook, instead of staring at a silently empty page; the empty-log message itself was rewritten to surface the underlying reason (file missing, file empty, all entries muted, filters too narrow) rather than the same generic line in every case; and admins triaging a flood can now select multiple groups in the grouped view and act on the selection at once — mute the lot in a single batch or export them to CSV for handoff to another tool. The mute path piggybacks on Phase 14's idempotent `/logs/mute` route and refetches the page after the batch so muted groups disappear immediately; the CSV path builds the file client-side from the already-fetched rows so no new server route is needed.

A new `Logscope\Support\DiagnosticsService` underpins both the banner and the empty-state copy: a typed-fields snapshot exposed through `GET /diagnostics` that captures `WP_DEBUG`, `WP_DEBUG_LOG`, the candidate log path, and whether the file exists with size and mtime. `LogViewer` fetches it once on mount; the same payload feeds the banner, the empty state, and (in a future revision) any other surface that needs to ask "is the host actually configured to log?" without firing its own request. Auto-edit of `wp-config.php` is intentionally out of scope here — the ROADMAP defers that to post-1.0.

### Added

-   Bulk actions in grouped view (Phase 16.5) — per-group checkbox at the start of every row plus a "Select all" checkbox in a new header bar above the list. The header carries a tri-state checkbox (unchecked / indeterminate when partial / checked) and a live "%d selected" counter, plus two bulk-action buttons that enable when at least one group is selected. **Mute selected** dispatches a new `bulkMuteSignatures` thunk on the `logscope/core` store which POSTs each signature in sequence to `/logs/mute` (cooperating with Phase 14's idempotent route — re-muting an already-muted signature is a no-op and a partial failure surfaces as a "Muted N of M; some failed" warning toast rather than aborting the batch); after the batch completes, `GroupedView` calls `fetchLogs` with the current filters + `grouped: true` so the muted groups drop out of view immediately rather than lingering until the next filter change. **Export selected** builds a CSV blob client-side from the rows already in the store (`severity, count, signature, sample_message, file, line, first_seen, last_seen` — RFC 4180 quoting via a small `csvCell` helper) and triggers a download through a temporary anchor element; the blob carries a UTF-8 BOM so Excel auto-detects encoding and a `logscope-groups-YYYYMMDD-HHMMSS.csv` filename so a habit of bulk-exporting does not silently overwrite the previous export. Selection state lives in `GroupedView` local component state — it is purely UI state, does not need to survive page navigation, and a `useEffect` prunes the set whenever the visible groups change so a muted-and-disappeared signature does not stay in the selection. Bundle weight: 75.5 KiB (was 70.5 KiB; +5 KiB for the bulk action bar, the CSV builder, and the new thunk).

### Changed

-   Reason-aware empty-log state (Phase 16.4) — `EmptyState` no longer renders the same "No log entries to show" message regardless of underlying cause. It now consumes the same `/diagnostics` snapshot the onboarding banner uses (already in the store from 16.3) plus the live mute count, and picks one of four reason-specific messages: filters excluded everything (existing), the configured log path does not exist (names the path), the file exists but is zero bytes (most common on a fresh install where logging is on but nothing has happened yet), or the log has bytes but every recent entry is muted out (suggests the Settings → Mute panel for review). The generic copy still fires as a fallback before the diagnostics fetch resolves and on hosts where `WP_CONTENT_DIR` is undefined and `log_path` is empty. `LogViewer` now also dispatches `fetchMutes` on mount so the mute-count branch is accurate on a Logs-tab cold start without waiting for the user to open Settings (the Mute panel still fetches independently — the thunk is idempotent enough that the panel sees the populated list either way). Bundle weight: 70.5 KiB (was 69.3 KiB; +1.2 KiB for the reason resolver).

### Added

-   Onboarding banner for missing `WP_DEBUG_LOG` (Phase 16.3) — new `OnboardingBanner` component rendered above the FilterBar in the Logs tab when the `/diagnostics` snapshot reports `wp_debug_log: false`. Copy explains that Logscope reads the file WordPress writes when `WP_DEBUG_LOG` is on, embeds the three exact lines to drop into `wp-config.php` (`WP_DEBUG`, `WP_DEBUG_LOG`, `WP_DEBUG_DISPLAY`), and links to the WordPress handbook's debugging article. Dismissed via the `<Notice>`'s built-in close button; the dismissal flag lives in `sessionStorage` (key `logscope:onboarding-dismissed`) rather than a store slice because it is purely UI state and should not be replayed across browser sessions — closing and reopening wp-admin gives the admin a fresh chance to act on it. The banner waits for `getDiagnostics()` to return a payload before deciding whether to render, so a fresh tab does not flash the warning while the snapshot is still in flight. `LogViewer` dispatches `fetchDiagnostics` once on mount; the same snapshot also feeds the upcoming reason-aware empty state (16.4) so we only fetch once. New `diagnostics` slice (`{data, isLoading, loadError}`), `fetchDiagnostics` thunk, `getDiagnostics` / `isLoadingDiagnostics` / `getDiagnosticsLoadError` selectors, and `client.getDiagnostics()` REST method against the Phase 16.2 endpoint. Auto-edit of `wp-config.php` is intentionally out of scope — the ROADMAP defers it to post-1.0. Bundle weight: 69.3 KiB (was 66.9 KiB; +2.4 KiB for the component, store slice, and styles).

-   `Logscope\REST\DiagnosticsController` (Phase 16.2) — `GET /diagnostics` over `DiagnosticsService`. Capability-gated through the shared `RestController` base, so an unauthenticated caller gets a 401 rather than a fingerprint of the host's debug-flag posture; the snapshot does leak the resolved log path and `WP_DEBUG` configuration but both are already visible to anyone with the manage capability through the existing settings surface, so the access bar matches. Wired through a new `support.diagnostics` factory in `Plugin::register_services()` (which constructs the service via `DiagnosticsService::from_environment()` against the same `path_guard` and `resolve_log_path()` that the existing `log_source` factory uses, so the snapshot reflects the file the plugin would actually read) and a `rest.diagnostics_controller` factory that hands the service to the controller. Slotted into the existing route-registration try/catch ladder so a misconfigured log path cannot abort `rest_api_init` for other plugins on the same tick — a swallowed exception writes a debug-gated breadcrumb to `error_log` matching the `logs` / `settings` / `alerts` / `mute` / `presets` / `stats` pattern. 3 integration tests cover the existing-log snapshot (with size + mtime populated), the missing-log branch (writable parent, zeroed size + mtime), and the route-registration shape.

-   `Logscope\Support\DiagnosticsService` (Phase 16.1) — produces a structured snapshot of host state relevant to the log viewer: `WP_DEBUG` / `WP_DEBUG_LOG` flags, the candidate log path the plugin would tail, whether that file exists, its size and modification time, and whether the parent directory is writable so a fresh log file could be created on demand. Every field is typed (boolean / int / string) and `null` is never returned, so the upcoming REST endpoint (16.2), onboarding banner (16.3), and reason-aware empty state (16.4) can all branch on simple truthiness rather than hand-rolled defensive type checks. Path resolution flows through `PathGuard`: a candidate that escapes the allowlist is reported as `exists:false` rather than throwing, so the diagnostics surface never propagates a path-validation failure to the caller. The boolean debug flags are constructor parameters rather than read from `WP_DEBUG` / `WP_DEBUG_LOG` directly because PHP constants cannot be undefined or re-defined inside a single PHPUnit process — production wiring will use the new `DiagnosticsService::from_environment()` static factory which reads the constants the same way `PathGuard::default_roots()` does. 5 unit tests cover the all-flags-off snapshot, the flags-on-but-file-missing branch (with `parent_writable` truthful), the populated-file branch (size + mtime), the outside-allowlist path being reported as missing, and the `from_environment()` factory's behaviour against the unit-test bootstrap (where neither constant is defined).

## [0.13.0] - 2026-04-30

Closes Phase 15 of the [roadmap](ROADMAP.md): a new **Stats** tab that gives an at-a-glance view of error frequency over time without leaving wp-admin. Server-side, `LogStats` reuses `LogParser` + `LogGrouper` over the same trailing 50 MiB read budget the repository uses, buckets entries by hour or day across a 24h / 7d / 30d window, and returns a `{range, bucket, buckets, totals, top}` payload through `GET /stats`. Results cache in a transient keyed by file size, mtime, range, bucket, and the snapped-now bucket boundary so a roll into the next bucket implicitly invalidates without requiring a write to the file. Mute filtering is intentionally **not** applied to stats — they are the ground truth of error volume; muting is a Logs-view noise control. Client-side, the new Stats tab slots between Logs and Settings (`Logs · Stats · Settings`) so the upcoming click-through from the top-N table populates the Logs FilterBar without leaving the visual neighbourhood.

The dashboard renders three coordinated views over the same payload: a **horizontal stacked breakdown bar** answering "what's the mix right now" (per-segment `flex-grow` so widths sum to 100% by construction), a **small-multiple sparkline grid** (hand-rolled SVG, no chart library) answering "how does each severity move over time" with bars normalised to per-chart peaks so a quiet severity is still readable next to a noisy one, and a **top-10 signatures table** with a "View in Logs" action that pre-populates the FilterBar with the row's severity plus a regex anchored on a 50-character prefix of the sample message — round-tripping the server's lossy normaliser from the client would either re-introduce that lossiness or require a parallel implementation, and the literal prefix is good enough to catch the same class of error in practice. Both the breakdown bar (`role="img"` + descriptive aria-label) and the sparkline cells (`<figure>` with peak / mean / total aria-label, SVG `aria-hidden`) carry text-equivalent summaries so the dashboard reads the same to AT users as it does at a glance.

### Added

-   Severity breakdown bar (Phase 15.6) — single horizontal stacked bar above the sparkline grid showing the proportion of each severity over the selected range. Where the sparkline grid answers "how does each severity move over time," this answers "what is the mix right now" — comparing magnitudes across severities, which the per-chart-peak normalisation in `Sparkline` deliberately does not do. Segments size via `flex-grow: <count>` rather than computed `width` percentages so the widths sum to 100% by construction and a re-render with one more entry redistributes proportionally without precision drift. Severities with zero entries are dropped from both the bar and the legend so a quiet-site bar doesn't render as an over-segmented stripe of empty slivers. The bar carries a `role="img"` and an aria-label of the form `"Severity breakdown over the range: <Severity1> NN%, <Severity2> NN%, …"` so AT users get the same proportional summary a sighted user gets from glancing. Legend below the bar lists each visible severity with its raw count and percentage. Bundle weight: 147 KiB built modules (was 144 KiB; +3 KiB for the component and styles).

-   Top signatures table with click-through to Logs (Phase 15.5) — new `TopSignaturesTable` rendered under the sparkline grid in the Stats tab. Each row shows severity (as the existing pill), the sample message, and the count; a "View in Logs" action dispatches `setFilters({severity: [row.severity], q: <regex>})` and switches to the Logs tab. The regex is built by escaping a leading 50-character slice of the sample message — round-tripping the server-side normaliser (which strips numbers, paths, hex addresses) from the client would either re-introduce that lossiness or require a parallel implementation, and a literal prefix of the sample is good enough to catch the same class of error in practice while keeping the click-through readable in the FilterBar (admins can refine from there). Tab switch goes through both `setActiveTab('logs')` and `window.location.hash = 'logs'` so the back button stays honest with the App's hashchange listener. Bundle weight: 144 KiB built modules (was 141 KiB; +3 KiB for the table component and styles).

-   Severity sparkline grid in the Stats tab (Phase 15.4) — hand-rolled SVG, no chart library; `Sparkline` renders a vertical-bar chart inside a fixed `120×32` viewBox with `preserveAspectRatio="none"` so the cell expands to fit its grid column without re-laying-out the bars themselves. Bars scale to the per-chart peak rather than the global peak so a quiet severity is still readable next to a noisy one — comparing magnitudes across severities is the bar in 15.6's job, not the sparkline grid's. The SVG itself is `aria-hidden` (the `<rect>` shapes carry no semantic content a screen reader could usefully voice); each chart is wrapped in a `<figure>` with an `aria-label` of the form `"<Severity>: <total> total, peak <peak>, mean <mean> per bucket"`, which gives AT users the same information density a sighted user gets from glancing at the bars. `SparklineGrid` only renders cells for severities with `totals > 0`, and the `unknown` bucket is dropped from the grid even when populated — "we couldn't classify this line" is not a severity an admin reasons about over time. Tones reuse the existing `severityTone()` mapping so fatal/warning/notice colours stay in lockstep with the Logs tab. Bundle weight: 141 KiB built modules (was 136 KiB; +5 KiB for the two components and the grid styles).

-   Stats React tab scaffold (Phase 15.3) — third tab between Logs and Settings (`Logs · Stats · Settings`); slotted between because Stats shares filter context with Logs and the click-through from the upcoming top-N table will populate the FilterBar without leaving the visual neighbourhood. New `stats` slice on the `logscope/core` store carries `{range, bucket, data, isLoading, loadError}` plus `setStatsRange` / `setStatsBucket` / `fetchStats` thunks; the fetch control reads the current range/bucket out of the store via the imported `select` so the tab does not have to thread them through the dispatch. New `client.getStats({range, bucket})` REST method against the Phase 15.2 endpoint. New `StatsTab` component with range and bucket toggle groups, a loading state, an error notice, and a placeholder summary row showing the total entry count for the selected window — the sparkline grid (15.4), top-N table (15.5), and severity breakdown bar (15.6) all consume the same `data` payload this scaffold already fetches, so the store + REST surface only land once. Tab order in `App.jsx` and the `TabContent` switch updated accordingly. Bundle weight: 136 KiB built modules (was 129 KiB; +7 KiB for the new component, store slice, and toggle UI).

-   `Logscope\REST\StatsController` (Phase 15.2) — `GET /stats?range=24h|7d|30d&bucket=hour|day` over `LogStats`. The args schema enumerates the allowed range and bucket tokens via `LogStats::ranges()` / `LogStats::buckets()` so unknown tokens are rejected by core's REST input validator before the handler runs; the `LogStatsException` catch is a defence-in-depth fallback for direct invocations that bypass the schema (tests cover both paths). Defaults are `range=24h` and `bucket=hour` so a Stats tab opening with no query string lands on the most common view. The route is wired through `Plugin::register_rest_routes()` inside the same try/catch ladder as the other route groups, so a misconfigured log source cannot abort `rest_api_init` for other plugins on the same tick — a swallowed exception writes a debug-gated breadcrumb to `error_log` matching the existing `logs` / `settings` / `alerts` / `mute` / `presets` pattern. New `log.stats` factory in the DI graph wires `LogStats` against the same `log_source` `FileLogSource` the repository uses, and a `rest.stats_controller` factory hands the service to the controller. 6 integration tests cover the default-param envelope shape, the 7d/day and 30d/day bucket counts, the wall-clock-relative totals path through a real `LogStats`, the args-schema enum + default contract, and the bad-range handler short-circuit.

-   `Logscope\Log\LogStats` (Phase 15.1) — time-bucketed aggregation service that powers the upcoming Stats tab. Builds a `{range, bucket, buckets[], totals, top}` summary over the configured log: per-severity counts in either hour or day buckets across a `24h`, `7d`, or `30d` window, plus a top-10 signature list whose entries are limited to the same window so the table and the bucket grid agree on the data they describe. Mute filtering is intentionally **not** applied — stats are the ground truth of error volume, and muting is a Logs-view noise control; a muted signature should still register in the totals so the admin sees what is actually happening on the site. Reuses `LogParser` and `LogGrouper` directly rather than routing through `LogRepository::query()` (which is shaped around pagination + filter application); inherits the same 50 MiB tail-read budget so a single tab open cannot blow the PHP memory*limit. Bucket boundaries snap down to the nearest hour or UTC day; entries with unparseable timestamps or timestamps outside the window are dropped. Results cache in a transient (`logscope_stats*<md5>`) keyed by `(size, mtime, range, bucket, snapped-now)`with a 60s TTL: the file's mtime widens the key beyond size so two writes that happen to land at the same byte count still mint distinct cache entries, and the snapped-now anchor means a roll into the next bucket implicitly invalidates without requiring a write to the file. The cache is bypassed entirely when the source is missing — there is no value in pinning a "log absent" verdict that becomes stale once the file appears. New`LogStatsException`for unknown range/bucket tokens (the future REST controller will map this to a 400).`FileLogSource`grows a`mtime()`accessor (kept off the interface to avoid forcing every future source implementation to define it;`LogStats`consults it via`method_exists`). 11 unit tests cover the empty-grid path, hour and day bucket assignment, out-of-window drop, unparseable-timestamp drop, top-N grouping over in-window entries, the TOP_N cap, the cache-hit short-circuit, mtime-driven cache-key change, the unknown-range and unknown-bucket exception paths, and the bucket-timestamp ordering.

## [0.12.0] - 2026-04-30

Closes Phase 14 of the [roadmap](ROADMAP.md): three features bundled into one release. Long-running sites can opt into **size-based log retention** (a daily cron renames `debug.log` to `debug.log.archived-YYYYMMDD-HHMMSS` once it crosses a threshold and prunes the oldest archives beyond a retention cap); admins can **mute noisy signatures** so a known-and-accepted error stops dominating the Logs view (with a Settings-tab management panel for unmuting); power users can **save filter presets** as a per-user list, recall them from a FilterBar dropdown, and overwrite or delete them in place. The mute pipeline filters at the repository layer so totals match the visible page; the preset payload captures `viewMode` alongside the filter values so a saved "grouped Akismet fatals" preset round-trips intact.

The retention path is opt-in (`retention_enabled` defaults off) so a fresh install does not silently rename a user's debug log; the same posture as the Phase 13 scan toggle. The mute filter is on by default but the management UI and the new `?include_muted=true` query string both bypass it. Presets are scoped to user meta — on a multi-admin site, named presets are personal triage tools, and surfacing a colleague's preset to everyone would be friction without value.

### Added

-   Filter preset save/load UI in FilterBar (Phase 14.10). New "Preset" dropdown above the Reset button: a "Load preset…" select-control listing the user's saved presets (preceded by a "No saved presets" placeholder option when the list is empty) plus a "Save preset" button that captures the current FilterBar state via `window.prompt` for the name. Inline `×` delete buttons next to each preset name with a `window.confirm` guard against fat-finger removals. Loading a preset dispatches `setFilters({severity, from, to, q, source})` + `setViewMode(viewMode)` so the captured grouped/list mode round-trips alongside the filter values. Inputs and outputs are routed through a new `presets` slice (`fetchPresets` / `savePreset` / `deletePreset` thunks, `getPresets` / `isSavingPresets` selectors) wired against the `/presets` endpoints from Phase 14.9. Bundle weight: 58.5 KiB (was 54.6 KiB; +3.9 KiB for the dropdown + thunks + REST methods).

-   `Logscope\REST\PresetsController` (Phase 14.9) — three routes over `PresetStore`: `GET /presets` returns the caller's preset list, `POST /presets` saves or overwrites by name (idempotent), `DELETE /presets/<name>` removes by URL-encoded name path segment (404 if missing). "Current user" semantics live in the controller via `get_current_user_id()` rather than in the store, so the store stays deterministic for unit tests; an unauthenticated caller gets a `401 logscope_rest_unauthenticated` rather than reading another user's presets via uid 0. Empty/whitespace names on POST and missing name on DELETE return `400 logscope_rest_invalid_preset_name`. Registered through the `rest.presets_controller` factory in `Plugin` and slotted into the existing route-registration try/catch ladder. 9 integration tests cover GET/POST/DELETE happy paths, idempotent overwrite, the 401 for each verb when unauthenticated, the 404 on unknown name, per-user isolation (preset saved as user A is not visible to user B), and the route registration shape.

-   `Logscope\Settings\PresetStore` (Phase 14.8) — per-user filter preset store backed by the `logscope_filter_presets` user-meta key. Each user owns their own list rather than sharing a global option: on a multi-admin site, presets are personal triage tools and surfacing a colleague's named preset to everyone would be friction without value. Storage shape is a numerically-indexed list of `{name, filters}`; `save()` overwrites by name rather than appending duplicates so re-saving "Akismet only" updates in place. The allowed filter key list is fixed (`severity`, `from`, `to`, `q`, `source`, `viewMode`) and enforced at the boundary so a future filter dimension cannot land in user meta without a schema bump and an old version of the plugin reading the row cannot trip over an unfamiliar key. `severity` is normalised to a unique list of strings; everything else is stringified or dropped if it arrives as an array. `MAX_NAME_LENGTH = 80` truncates pathological labels rather than rejecting them so the dropdown layout stays predictable. Registered as `settings.preset_store` in the DI graph for 14.9 to consume. 11 unit tests cover the round-trip, name overwrites, name trim/truncate, empty-name + non-positive-user-id rejection, both `delete()` paths, the corruption-tolerant loader, and the severity/array-typed-string sanitisers.

-   Mute UI on grouped rows + Settings management panel (Phase 14.7). New "Mute" button on each `GroupedView` row dispatches `muteSignature(signature, reason)` after collecting an optional reason via `window.prompt` — the smallest modal that satisfies the AC without pulling a bespoke `<Modal>`'s portal/focus-trap surface, and the management panel is where reasons get edited at length anyway. New `MutedSignaturesPanel` rendered at the bottom of the Settings tab fetches the list on mount, shows each `signature` + `reason`, and offers an "Unmute" button per row. New `mutes` slice in the `@wordpress/data` store carries `{ items, isLoading, isSaving, loadError, saveError }` plus thunks `fetchMutes()` / `muteSignature()` / `unmuteSignature()` that wrap the new `client.getMutes()`, `client.muteSignature()`, `client.unmuteSignature()` REST methods. Toast feedback on success/failure mirrors the Settings tab's pattern. Bundle weight: 54.6 KiB (was 50.4 KiB; +4.2 KiB for the panel + actions + selectors).

-   `LogRepository` filters muted signatures by default (Phase 14.6). New optional `MuteStore` constructor argument; absent store preserves the pre-14.6 behaviour for legacy direct callers (test fixtures), while the production DI graph wires the same `log.mute_store` instance the REST controller writes to. New `LogQuery::$include_muted` flag (default false) — opt in via `?include_muted=true` for the management UI. Filtering happens after the existing severity/source/regex/date filters and before grouping + pagination, so totals match the visible page; tail mode skips muting entirely (delta polling is for the live feed). Both ungrouped (`Entry[]`) and grouped (`Group[]`) paths consult the same lookup map; `LogsController::build_query()` threads the new param through with the same `(bool)` coercion the existing toggles use. 4 new repository integration tests cover the default-filtered ungrouped + grouped paths, the `include_muted` bypass, and the no-store fallback; 1 new controller test asserts `build_query` reads the param.

-   `Logscope\REST\MuteController` (Phase 14.5) — three routes over `MuteStore`: `GET /logs/mute` returns the mute list, `POST /logs/mute` adds or updates a record (idempotent — re-muting the same signature updates in place rather than 409-ing), `DELETE /logs/mute/<signature>` removes by signature path segment (404 if not muted). Signature is in the URL path on DELETE rather than the body because DELETE bodies are unevenly supported by middleware. Permission gate is the same `logscope_manage` capability check inherited from `RestController::permission_callback()` as the other Logscope routes; `handle_post` rejects empty/whitespace signatures with `400 logscope_rest_invalid_signature` rather than silently no-oping. Registered through the `rest.mute_controller` factory in `Plugin` and registered alongside the other route groups in `register_rest_routes()`. 8 integration tests cover GET/POST/DELETE happy paths, the idempotent POST, the 400 on missing/empty signature, the 404 on unknown signature, and the route registration shape.

-   `Logscope\Log\MuteStore` (Phase 14.4) — typed CRUD wrapper over the `logscope_muted_signatures` option. Storage shape is an associative map keyed by signature so existence checks and re-mute updates are O(1) without a second index; each record carries `signature`, `reason` (HTML-stripped via `wp_strip_all_tags` defense-in-depth), `muted_at` (Unix timestamp), and `muted_by` (acting user id; 0 for non-user contexts). `add()` updates an existing record in place rather than appending a duplicate, `remove()` returns a bool so callers can map the result to a 404 on the future REST DELETE, `list()` returns the records as a numerically-indexed array (the keyed map is internal), `signatures()` returns the keys-only view used by `LogRepository` filtering (Phase 14.6). The loader defends against corruption from a prior version by dropping non-array entries rather than throwing. Registered in the DI graph as `log.mute_store`. 12 unit tests cover the persistence shape, HTML stripping, in-place update, empty-signature rejection, negative-user-id clamp, both `remove()` paths, list/signatures/is_muted, and the corruption-tolerant loader.

-   `logscope_rotate_logs` cron event + lifecycle (Phase 14.3). `CronScheduler` grows `apply_rotation()` / `clear_rotation()` mirroring the scan-event pattern but using WP-Core's built-in `daily` recurrence — retention does not benefit from sub-daily checks, and reusing the existing recurrence avoids a second `cron_schedules` filter entry. New constants `HOOK_ROTATE` (`logscope_rotate_logs`), `RECURRENCE_ROTATE` (`daily`), `OPT_RETENTION_ENABLED`. `Plugin` registers a `cron.rotator` factory that wires `LogRotator` against the same `log_source` + `path_guard` services as the scanner and converts `retention_max_size_mb` to bytes at the boundary; the `logscope_rotate_logs` action listener catches `Throwable` from DI-graph construction so a misconfigured log path cannot abort other plugins' scheduled events on the same tick. Re-alignment is wired through `update_option_<key>` / `add_option_<key>` listeners on `logscope_retention_enabled` via a new `Plugin::on_retention_setting_changed()` callback. `Activator::activate()` calls `apply_rotation()` after seeding defaults so re-activation rebuilds the daily hook if it was lost; `Deactivator::CRON_HOOKS` adds the rotate hook so deactivation clears both events. 3 new unit tests cover the disabled→clear path, the enabled→clear+reschedule path (asserting the `daily` recurrence + the explicit clear-before-schedule pair), and the unconditional `clear_rotation()`.

-   `SettingsSchema` retention fields (Phase 14.2). Three new settings: `retention_enabled` (int 0/1, default 0; bool→int coercion shared with the alert + cron toggles), `retention_max_size_mb` (int, default 50, clamped to [1, 1024]), `retention_max_archives` (int, default 5, clamped to [1, 50]). Both clamps mirror the cron-interval shape — a 1-floor so a typo cannot defeat retention via a side door, and a hard ceiling so a typo cannot suspend rotation for an unreasonable span. `Activator::DEFAULT_OPTIONS` seeds the three new keys; `Settings::all()` shape grows from nine to twelve fields. The shape change is propagated through `SettingsSchemaTest::test_keys_returns_declared_field_names`, `SettingsTest::test_all_returns_full_settings_shape`, and the three `SettingsControllerTest` shape assertions. 3 new sanitiser tests cover the bool→int coercion + the two clamps.

-   `Logscope\Log\LogRotator` (Phase 14.1) — size-based archival of the live log. Renames `debug.log` to `debug.log.archived-YYYYMMDD-HHMMSS` (UTC) once the file size meets a configured threshold, then prunes oldest archives by `mtime` beyond a configurable retention cap. Stateless — every tick re-derives the archive list, so concurrent writes between ticks cannot leave the rotator with a stale view. Both the pre-rename writability check and the prune loop are gated by `PathGuard::is_writable_parent_of()`. Filesystem failures (missing file, non-writable parent, failed `rename`, same-second collision with an existing archive) all return a structured noop (`array{archived_to: ?string, pruned: string[], skipped: bool}`) rather than throwing, so a future cron callback can finish cleanly. Threshold takes bytes rather than MB so unit tests can exercise small fixtures without integer-MB rounding; the SettingsSchema MB→bytes conversion will live at the wiring site (Phase 14.3). 6 unit tests cover the size-below-threshold no-op, the missing-file no-op, the over-threshold archive path, the prune-oldest path with 5 pre-existing archives, the within-cap no-prune path, and zero-threshold/zero-cap defensive no-ops.

### Fixed

-   `PathGuard::resolve()` docblock now declares both `@throws` tags (`InvalidPathException` for malformed/escaping input, `MissingPathException` for unresolvable paths) — the second was missing since Phase 11 and surfaced once `composer lint` started enforcing `Squiz.Commenting.FunctionCommentThrowTag.WrongNumber`.

## [0.11.0] - 2026-04-30

Closes Phase 13 of the [roadmap](ROADMAP.md): the producer that drives Phase 12's alert pipeline. A new `LogScanner` runs on a configurable WP-Cron schedule (off by default; opt-in via the Settings tab), reads the slice of the log appended since the last tick, filters parsed entries down to `fatal` and `parse` severities, groups them, and dispatches the resulting groups through the existing `AlertCoordinator`. Rotation handling, an in-memory cursor, a per-tick timestamp, and a per-tick dispatched count round out the scanner; the React Settings UI grows a "Scheduled scan" section with the toggle, interval input (1–1440 minute clamp), and a "Last scan: …" status line. The lifecycle is fully owned by a new `CronScheduler` so toggling the option in the UI, the activation hook, and a future WP-CLI path all converge on the same schedule.

The integration test landed in this phase wires a real `AlertCoordinator` with a tiny capturing dispatcher and triggers the scan via `do_action('logscope_scan_fatals')` so a regression that disconnects the action callback would surface immediately rather than passing through unit-level mocks.

### Added

-   `LogScannerIntegrationTest` — end-to-end coverage of the `do_action('logscope_scan_fatals') -> LogScanner -> AlertCoordinator -> dispatcher` pipeline (Phase 13.5). Brain Monkey records `add_action` / `do_action` for assertions but does not actually invoke registered callbacks, so the test stubs both onto a per-test callback registry. With that wiring in place, calling `do_action('logscope_scan_fatals')` drives `LogScanner::scan()` the same way WP-Cron would in production. Two integration tests cover the happy path (two-fatal fixture → real `AlertCoordinator` + a tiny capturing `AlertDispatcherInterface` implementation observes both groups, cursor and dispatched-count options advance) and the no-new-bytes second invocation (capture stays at 1, dispatched count drops to 0). Closes the loop the unit suite leaves open: a regression that disconnects the action callback now fails this test rather than passing through unit-level mocks.

-   React `CronPanel` and bootstrap status surface (Phase 13.4) — new "Scheduled scan" section under the Settings tab with a `ToggleControl` for the master enable, a numeric `TextControl` for the interval (1–1440 minute clamp matching the schema), and a read-only "Last scan: <relative time> · <N> fatals dispatched" status line. The interval input is hidden until the toggle is on so the form stays uncluttered for admins who don't want a scheduled scan. Cron fields share the existing settings draft slice — there's still one Save button at the bottom of the Settings tab — so the dirty-check now spans nine fields. Last-scan status is exposed page-load-style through `wp_localize_script` (new `cronStatus` key carrying `lastScannedAt` + `lastScannedDispatched`) rather than via a polling REST endpoint: scan summaries are scanner-internal state rather than user-editable settings, save-then-reload picks up the latest tick, and avoiding a polling endpoint keeps the wire surface minimal. The relative-time formatter degrades gracefully across seconds → minutes → hours → days using `_n()` for plural-aware translations, and the "never scanned" branch shows "Last scan: never" until `cronStatus.lastScannedAt` is populated. Bundle weight: 50.4 KiB (was 47.7 KiB; +2.7 KiB for the panel + relative-time helper). 1 new AssetLoader test covers the populated `cronStatus` payload; the existing localized-payload test asserts the empty-state shape.

-   `SettingsSchema` cron fields + `cron_schedules` filter (Phase 13.3). Two new settings: `cron_scan_enabled` (int 0/1, default 0; same bool→int coercion path the alert toggles use so a future REST client passing `"on"` / `"true"` / `"1"` round-trips cleanly through `wp_options`), and `cron_scan_interval_minutes` (int, default 5, clamped to [1, 1440] — 1-minute floor because anything sub-minute is fictional given WP-Cron's hook-flush cadence; 1440-minute ceiling so a typo cannot silently suspend scanning for a longer stretch). `Plugin::register_cron_schedule()` is wired onto the core `cron_schedules` filter and registers the `logscope_scan_interval` recurrence at `interval = minutes * 60`; the same [1, 1440] clamp is re-applied at the filter boundary as defense-in-depth so a corrupted row from a prior version cannot land a 0-second recurrence. The `display` label uses `_n()` so "Every 1 minute (Logscope)" / "Every 5 minutes (Logscope)" pluralise correctly. The `Settings::all()` shape now carries seven core + alerts + cron fields (nine total); `SettingsControllerTest`, `SettingsTest::test_all_returns_full_settings_shape`, and `SettingsSchemaTest::test_keys_returns_declared_field_names` are updated for the new shape. 7 new tests cover the bool→int coercion on `cron_scan_enabled`, the [1, 1440] clamp on `cron_scan_interval_minutes`, and the filter callback (happy path, low-clamp, high-clamp, non-array argument tolerance, existing-schedules preservation).

-   `Logscope\Cron\CronScheduler` and lifecycle wiring (Phase 13.2). The scheduler is the single owner of the `logscope_scan_fatals` event — `apply()` reads `logscope_cron_scan_enabled` + `logscope_cron_scan_interval_minutes` directly via `get_option` (so it's safe to call from the activation hook before the DI graph exists), then either unschedules (toggle off) or clear-and-reschedules (toggle on) at the `logscope_scan_interval` recurrence. The clear-then-schedule pair is intentional: re-using `wp_schedule_event` on an existing event keeps the original cadence, so an interval bump would otherwise be silently absorbed. `Activator::activate()` calls `apply()` after seeding defaults; `Deactivator::deactivate()` already calls `wp_clear_scheduled_hook` for the event and now references the scheduler's hook constant so the name lives in one place. Re-scheduling on toggle is wired through `update_option_<key>` and `add_option_<key>` listeners on both option keys via a static `Plugin::on_cron_setting_changed()` callback that simply calls `CronScheduler::apply()`. The activation seed adds two new defaults: `logscope_cron_scan_enabled` (0 — opt-in, so a fresh install does not start cron noise) and `logscope_cron_scan_interval_minutes` (5). Both are also surfaced through `SettingsSchema` in Phase 13.3; until then the scheduler reads them with hardcoded fallbacks. 3 unit tests cover the disabled→clear path, the enabled→clear+reschedule path (including the explicit interval re-application), and the unconditional `clear()`.

-   `Logscope\Cron\LogScanner` (Phase 13.1) — background scanner that reads the slice of the log appended since the last tick, filters parsed entries down to `fatal` and `parse` severities, groups them, and dispatches the resulting groups through `AlertCoordinator::dispatch_for_groups()`. Cursor is the `logscope_last_scanned_byte` option; the run timestamp lands in `logscope_last_scanned_at` and the per-run group count in `logscope_last_scanned_dispatched` so the Settings UI can show "Last scan: …" without inventing a parallel summary store. Rotation is detected by a shrunk file (current `size < last_byte`) — the cursor resets to 0 and the new file is rescanned from the start, with the shrink signalled in the structured return as `rotated: true`. No-op when no new bytes arrived (still updates the timestamp so the UI doesn't show a stale "Last scan"). Wired into `Plugin` as the `cron.scanner` service plus an `add_action('logscope_scan_fatals', ...)` listener on the same `register_hooks()` pass as `rest_api_init` / `admin_menu`; the callback resolves through the same DI graph as the REST controllers and traps `Throwable` so a misconfigured log path does not abort other plugins' scheduled events on the same tick. 4 unit tests cover the two-fatal dispatch path, the no-new-bytes no-op, the rotation reset, and the severity filter (warning/notice are dropped).

### Changed

-   `Logscope\Alerts\AlertCoordinator` is no longer `final`. The cron scanner's unit tests need a Mockery double to assert `dispatch_for_groups()` interaction; matching the precedent set by `AlertDeduplicator` and `SettingsSchema`.

## [0.10.0] - 2026-04-30

Closes Phase 12 of the [roadmap](ROADMAP.md): the alerts subsystem. Logscope can now notify admins about new fatals via email, webhook, or both — fanned out by a single `AlertCoordinator` that applies signature-keyed dedup per-dispatcher (so silencing email on a noisy fatal does not silence the webhook on the same fatal). Five new settings drive the surface (`alert_email_enabled`, `alert_email_to`, `alert_webhook_enabled`, `alert_webhook_url`, `alert_dedup_window`); the React Settings tab grows an `AlertsPanel` section with a "Send test alert" button that hits a new `POST /wp-json/logscope/v1/alerts/test` endpoint. The webhook dispatcher is the first surface introducing outbound HTTP — hardened with a two-layer http(s) scheme allowlist (settings sanitiser + dispatcher runtime check), `redirection: 0` against SSRF via 30x bounce, and a 5s timeout. A full `security-review` skill pass on the alerts surface returned zero HIGH/MEDIUM findings.

The pipeline is in place but not yet driven by anything in the plugin — Phase 13 (scheduled fatal scanner) is what actually pulls fatals from the log on a cron schedule and feeds them to the coordinator. v0.10.0 ships the dispatch path + UI; v0.11.0 will ship the producer.

### Added

-   React `AlertsPanel` and store wiring (Phase 12.7) — new section under the Settings tab with `ToggleControl`s for email and webhook backends, conditional `TextControl`s for the recipient address and webhook URL (revealed only when the corresponding toggle is on so the form stays uncluttered for admins who don't want alerts), a numeric `TextControl` for the dedup window with a 60s `min`, and a "Send test alert" button. The button stays disabled until at least one backend is enabled with an inline hint ("Enable email or webhook above first, then save."), and per-dispatcher results render as colour-coded `Notice` rows after a test (`sent` → success, `failed` → error, `skipped`/`deduped` → info). Alert fields share the existing settings draft slice — there's still one Save button at the bottom of the Settings tab — so the dirty-check now spans seven fields and the save POST sends the full body. New store actions: `startSendingTestAlert`, `receiveAlertTestResults`, `failAlertTest`, `clearAlertTestResults`, and the `*sendTestAlert` generator that fires a success or warning toast based on whether any backend reported `sent`. New selectors: `isSendingTestAlert`, `getAlertTestResults`, `getAlertTestError`. New API client method: `client.testAlert()`. Bundle weight: 47.7 KiB (was 45.2 KiB; +2.5 KiB for the panel + 5 new translatable strings).
-   `Logscope\REST\AlertsController` and `POST /wp-json/logscope/v1/alerts/test` (Phase 12.6) — backs the Settings UI's "Send test alert" button. Builds a synthetic group with severity `fatal`, signature `logscope-test-alert`, and a clearly-marked test message, then dispatches it to every enabled backend via `AlertCoordinator::dispatch_one()` with `bypass_dedup=true`. Returns 400 `logscope_rest_no_alerters_enabled` when no backend is enabled (so the UI can surface "nothing to send to" rather than silently claim success), or 200 with `{results: [{dispatcher, signature, outcome}]}` per dispatcher. The bypass+clear pattern means clicking the test button twice produces two real sends, and a real fatal arriving inside the dedup window after a test is not silently suppressed by it. Wired into `Plugin::register_services()` as `rest.alerts_controller` and registered in `register_rest_routes()` in its own `try/catch` so a failure here cannot abort the logs or settings route registration. 4 integration tests cover the 400-when-disabled branch, the per-dispatcher dispatch results, the dedup-bypass through two consecutive sends, and the route registration shape. Existing `SettingsControllerTest` shape assertions updated for the seven-field response.

-   `SettingsSchema` alert fields and `Plugin` DI wiring for the alerts subsystem (Phase 12.5). Five new settings: `alert_email_enabled` (int 0/1), `alert_email_to` (string, sanitised through `sanitize_email`), `alert_webhook_enabled` (int 0/1), `alert_webhook_url` (string, `esc_url_raw` + http(s) scheme allowlist mirrored from `WebhookAlerter` as defense-in-depth at the settings boundary), `alert_dedup_window` (int seconds, default 300, min 60 — same floor `AlertDeduplicator` enforces). Boolean toggles persist as `0`/`1` integers rather than booleans because the `wp_options` layer round-trips integers cleanly across MySQL encodings; booleans serialise as `''`/`'1'` which is fragile. `Activator::DEFAULT_OPTIONS` seeds the new keys on activation. `Plugin::register_services()` registers `alerts.deduplicator`, `alerts.email`, `alerts.webhook`, and `alerts.coordinator` services that read their config from `Settings` at construction — matches how `FileLogSource` resolves its log path and keeps the alerter classes pure for testing. 4 new schema tests cover the bool→int coercion (truthy strings: "true", "1", "yes", "on"), the email sanitiser passthrough, the webhook URL scheme allowlist (rejects `file://`), and the dedup-window floor (60s minimum, 300s default on non-numeric input). Updated `SettingsSchemaTest::test_keys_returns_declared_field_names` and `SettingsTest::test_all_returns_full_settings_shape` for the new field set.

-   `Logscope\Alerts\AlertCoordinator` (Phase 12.4) — fans a grouped error out to every registered dispatcher and applies dedup per-dispatcher so a webhook can fire while email is rate-limited (and vice versa). Disabled dispatchers and deduped signatures are skipped without invoking `dispatch()`. Two filterable hooks bracket each send: `logscope/before_alert` (filter; return `false` to skip — gives extensions a custom-mute hook that doesn't have to mutate the deduplicator) and `logscope/alert_sent` (action, fires only on success). `dispatch_one()` exposes a `$bypass_dedup` flag used by the test-alert endpoint that skips the `should_send` check and clears any existing dedup mark on success so a subsequent real fatal in the window is not silently suppressed by the test send. Returns a structured per-call summary (`{dispatcher, signature, outcome}`) where `outcome` is one of `sent` / `deduped` / `skipped` / `failed`. 8 unit tests cover the disabled gate, dedup short-circuit, success path with `record_sent` + `alert_sent`, failure path (no record), `before_alert` short-circuit, independent dedup across dispatchers, and the bypass+clear branch.

### Changed

-   `Logscope\Alerts\AlertDeduplicator` is no longer `final`. The coordinator's unit tests need a Mockery double to assert the dedup interaction; matching the precedent set by `SettingsSchema` (un-finaled in Phase 5 review for the same reason).

### Added

-   `Logscope\Alerts\WebhookAlerter` (Phase 12.3) — `wp_remote_post`-backed dispatcher with a neutral JSON payload (`{site, url, severity, message, file, line, signature, count, first_seen, last_seen}`) so site owners can adapt for Slack / Discord / Teams / PagerDuty via the `logscope/webhook_payload` filter rather than waiting for first-class formatters. Calls go out with `timeout: 5` (a slow webhook cannot stall the pipeline), `redirection: 0` (a 30x from the configured host cannot bounce the request to an internal address), and `blocking: true`. Two-layer URL hardening: `wp_http_validate_url()` rejects malformed URLs, then a scheme allowlist refuses anything that isn't `http(s)://` so a configured `file://` cannot smuggle a local-file read into the HTTP API. Non-2xx responses and `WP_Error` results both return false without throwing — the coordinator records the outcome and continues with remaining backends. The payload filter accepts any return shape (Slack message blocks, Discord embeds, …); a non-array return reverts to the default to keep a misbehaving filter from silently dropping alerts. 10 unit tests cover the URL-validation refusal, scheme allowlist, response-code branches, and filter integration.

### Security

-   Webhook dispatcher is the first surface introducing outbound HTTP. Hardening notes in the docblock cover SSRF defense (scheme allowlist + `redirection: 0`) and DoS defense (5s timeout). The `security-review` skill pass at Phase 12.8 will revisit the surface end-to-end.

-   `Logscope\Alerts\EmailAlerter` (Phase 12.2) — `wp_mail()`-backed dispatcher that sends one HTML email per grouped error with a plaintext alternative attached via the `phpmailer_init` action so on-call tools that strip HTML still get readable content. Subject is `[Logscope] <Severity> on <site>: <60-char snippet>`; long messages are truncated with an ellipsis since Outlook clips around 75 chars on mobile and the file/line is more useful to keep. The HTML body is a hand-rolled `<table>` of severity / message / location / occurrences / first-seen / last-seen / site (no template engine — ~150 bytes of markup doesn't justify the dependency surface). `logscope/email_subject` and `logscope/email_body` filters let site owners reshape either before send; the body filter receives an `{html, plain}` array and returning a non-array reverts to the defaults so a misbehaving filter cannot drop the alert silently. CRLF in the recipient or subject is refused even though `sanitize_email` already runs at the settings boundary — defense-in-depth against header injection if a future filter or code path slips one through. The dispatcher takes scalar config (`bool $enabled`, `string $to`) rather than a Settings dependency, matching how `FileLogSource` reads its path: the DI factory in `Plugin` will read the option and pass the resolved values in.
-   `Logscope\Log\Severity::label()` static helper that returns the translated, display-form label for a severity token (`fatal` → "Fatal error", etc.). Mirrors the JS `severityLabel()` helper in `assets/src/utils/severity.js` so PHP-side surfaces (alerts, future CLI output) read the same way as the React UI without each caller maintaining its own mapping.

-   `Logscope\Alerts\AlertDispatcherInterface` and `Logscope\Alerts\AlertDeduplicator` (Phase 12.1) — interface + signature-keyed dedup window backing the alert pipeline. `AlertDispatcherInterface` declares `name()` (stable backend id), `is_enabled()` (settings-driven gate the coordinator checks before invoking dispatch), and `dispatch(Group $group): bool` (returns `false` on transport failure rather than throwing, so the coordinator can record the outcome and continue with the remaining backends). `AlertDeduplicator` keeps a separate transient-backed window per `(dispatcher_name, signature)` pair so silencing email on a noisy fatal does not silence the webhook on the same fatal. Window length is configurable with a 60-second floor (anything shorter defeats dedup on a noisy site). The transient prefix is `logscope_alert_<dispatcher>_<sig>` and stays well under WP's 172-char key limit. A `clear()` helper drops the mark — used by the `/alerts/test` endpoint so an admin clicking "Send test alert" twice in quick succession is never silently rate-limited.

### Changed

-   **Roadmap restructure (2026-04-30).** Five feature phases were inserted before the wp.org cut so the plugin ships with more than a viewer-only MVP. New Phases 12 (Alerts), 13 (Scheduled fatal scanner), 14 (Retention + mute + filter presets), 15 (Stats dashboard), and 16 (Onboarding + diagnostics + bulk actions) replace what was previously a thin "release infrastructure → cut v1.0.0" phase. Alerts and the cron scanner were pulled forward from post-1.0 (originally v1.1.0 / v1.2.0). The original Phase 12 content (readme.txt, wp.org assets, release workflow, security gate, v1.0.0 cut, wp.org submission) moved verbatim to Phase 17 with no scope change. Version line: 0.10.0 → 0.14.0 close Phases 12–16, 0.15.0 closes Phase 17.1–17.4 release infra, optional 0.x.0 bumps cover Phase 17.5–17.6 pre-1.0 changes, and 1.0.0 closes Phase 17.7–17.11. See [ROADMAP.md](ROADMAP.md) for the full breakdown and [AGENTS.md §12](AGENTS.md#12-build-order) for the mirrored summary.

## [0.9.0] - 2026-04-28

Closes Phase 11 of the [roadmap](ROADMAP.md): polish, accessibility, dark mode, and i18n. The Logscope admin page picks up its first dedicated stylesheet (variable-driven for light / OS-dark / `admin-color-midnight`), loading skeletons replace the bare spinner on initial fetch, REST failures and save successes surface as `@wordpress/components` Snackbars from a new `toasts` slice, four global keyboard shortcuts (`/`, `g`, `t`, `?`) land alongside WAI-ARIA-compliant tablist arrow-key navigation, and `languages/logscope.pot` now covers all 87 PHP and JS strings. With Phase 11 done the next milestone is the v1.0.0-rc.1 cut: `readme.txt`, the wp.org banner / icon / screenshot set, and the release workflow that strips dev assets from the published zip.

### Added

-   Phase 11.1–11.5: polish, accessibility, dark mode, and i18n. The Logscope admin page picks up its first dedicated stylesheet (`assets/src/style.scss`, imported from the React entry); previously every `logscope-*` class name was a semantic hook with no rules attached and the page inherited whatever wp-admin shipped, which left rows visually undifferentiated and gave dark-mode users on `admin-color-midnight` an effectively unstyled UI. The new sheet is variable-driven so component rules reference `--logscope-*` tokens exclusively and every theme variant — light, OS dark via `prefers-color-scheme`, and the WP `admin-color-midnight` admin scheme — is one variable-block remap rather than a parallel rule set, keeping the cascade as the single source of truth. A `prefers-reduced-motion` block disables the skeleton shimmer for users who opt out.
-   `ListSkeleton` / `FormSkeleton` components (Phase 11.1) — replace the bare `<Spinner />` previously rendered on the initial fetch with shimmering placeholder rows that hint at the eventual shape of the list (or form). Spinners gave no sense of layout, so the page looked broken between the request kick-off and the first byte; skeletons keep the user oriented. Both wrappers render `role="status"` + `aria-live="polite"` so screen readers announce the loading state, while the inner skeleton bars are `aria-hidden` to avoid the AT layer narrating a stack of empty divs.
-   Toast / Snackbar surface (Phase 11.1) — new `toasts` slice on the `logscope/core` store + a `ToastHost` component mounted at the App root that renders the queue as `@wordpress/components` Snackbars. The `pushToast` action is fired from every REST failure path in the store (`fetchLogs`, `saveSettings`, `testLogPath`) so a transport blip surfaces as an explicit cue rather than just a silent `error` field on the slice; settings save also fires a `success` toast so the admin gets confirmation without having to read the page for state diff. Each toast carries a self-pruning `expiresAt` timestamp the host turns into a single-effect timer chain, and `dismissToast` is wired to the Snackbar's `onRemove` so click-to-dismiss races cleanly with the auto-prune.
-   Empty-state copy is now filter-aware (Phase 11.1). `EmptyState` takes a `filtersActive` flag and the LogViewer derives it from the live filter slice; instead of a generic "No log entries to show." an admin staring at the screen after a typo in the regex sees "No entries match the current filters. Try widening the date range or clearing the regex." — actionable rather than a dead end. The error variant keeps `role="alert"` for immediate AT announcement; the empty variant uses `role="status"` with a polite live region.
-   Keyboard shortcuts (Phase 11.2). New `useKeyboardShortcuts` hook bound at App level: `/` focuses the regex search input (also selects the existing value so a power user can replace-by-typing), `g` toggles list ↔ grouped, `t` toggles tail mode on/off, and `?` opens a help modal listing the bindings. Shortcuts are gated on `event.ctrlKey || metaKey || altKey` (so `Ctrl+T` still opens a browser tab) and on the focused element being a non-editable target (so typing `g` into the regex field is a literal `g`, not a view toggle). The hook is decoupled from the components that handle each shortcut: App emits a `logscope:shortcut` `CustomEvent` and `LogViewer` / `FilterBar` subscribe to the events that target their DOM. This keeps the global key listener (which has to live above the tab switch) free of knowledge about specific refs and lets each handler colocate with the state setters or focus targets it manipulates. Pressing `g` or `t` from the Settings tab auto-switches to the Logs tab first, so the keystroke can't be silently dropped on the wrong screen. A "Shortcuts (?)" button in the tab bar opens the same modal for users who don't read changelog entries.
-   `HelpModal` component (Phase 11.2). Built on `@wordpress/components` Modal, which gives focus trapping, escape-to-close, and `aria-labelledby` for free. The shortcut list is a `<dl>` of `<kbd>` elements; the trailing note ("Shortcuts are ignored while typing in an input.") explains the gating so users don't think `?` is broken when nothing happens from inside the regex field.
-   Accessibility pass (Phase 11.3). Tab buttons now carry roving `tabIndex` (active = 0, inactive = -1) so a keyboard user can `Tab` through the page without landing on every inactive tab. The regex search input picks up an explicit `aria-label` ("Search log messages (regex)") because the visible "Search (regex)" label disappears under the field at narrow widths; the aria copy is the durable contract. The skeleton wrappers and the empty-states wire `role="status"` / `role="alert"` + `aria-live` regions appropriately so AT users get the same loading / empty / error cues a sighted user does. A global `:focus-visible` ring lands inside `.logscope-app` so the whole admin page picks up a consistent 2px outline (instead of relying on whatever wp-admin's reset leaves in place); the ring uses the `--logscope-focus-ring` variable and remaps in dark mode along with the rest of the palette. The `Shortcuts` toggle button carries an `aria-label` distinct from its visible "Shortcuts (?)" caption so screen readers don't read out the literal punctuation.
-   Dark-mode parity (Phase 11.4). `@media (prefers-color-scheme: dark)` flips the entire palette to a dark variant tuned against `#1d2327` (WordPress's canonical dark-admin background) — text, borders, severity-pill tones, skeleton shimmer, focus ring, and the Snackbar host all rebase off the same `--logscope-*` variables. A `body.admin-color-midnight` selector additionally overrides the OS preference for users who explicitly picked WordPress's only first-party dark admin scheme, since "I picked midnight in my profile" is a stronger signal than `prefers-color-scheme`. The light/dark severity-pill tones are tuned for WCAG AA contrast on each background so a `Fatal error` pill stays readable in both modes.
-   i18n pass (Phase 11.5). Audited every user-facing string across `assets/src/**` and `src/**`; all toast messages, skeleton aria-labels, shortcut descriptions, and the help-modal copy are wrapped in `__()` / `_n()` / `sprintf()` with translator notes where placeholders carry meaning. New `languages/logscope.pot` (87 messages, both PHP and JS strings — neither `wp-pot-cli` nor `wp-scripts` extracts both languages in one pass, so the build uses WP-CLI's `i18n make-pot`). Generated by a new `pnpm i18n` script that downloads `wp-cli.phar` to a gitignored `tools/` directory on first run rather than pulling `wp-cli/wp-cli-bundle` (~150 MB of Composer dev deps); PHP must be on PATH but the plugin already requires PHP ≥ 8.0 for development. The script lives in `bin/make-pot.mjs` (export-ignored, so it does not ship in the wp.org zip).

### Changed

-   Phase 11 review follow-ups. **Tabs arrow-key navigation** — `App` now binds `onKeyDown` on the `role="tablist"` element so Left / Right cycle between tabs and Home / End jump to the first / last; previously a keyboard user could only `Tab` into the active tab and `Tab` back out, which left a real gap against the Phase 11.3 "keyboard nav for all interactive elements" promise (axe-core wouldn't have caught it — the structure was correct, the behaviour wasn't). Each tab carries a `data-logscope-tab` attribute the handler queries on the next frame to move focus to the newly-active tab, since the roving `tabIndex` flips on the very next render. **Shortcut-event constants extracted** — the `SHORTCUT_EVENT` name and the three event-detail strings (`focus-search`, `toggle-grouped`, `toggle-tail`) now live in `assets/src/shortcuts.js` rather than being re-exported from `App.jsx` and inlined as string literals across `LogViewer` and `FilterBar`. The previous shape worked through ESM hoisting (App imported the leaves and the leaves imported `SHORTCUT_EVENT` back from App), but the bidirectional graph was brittle and the inline literals were typo-prone. **HelpModal switch removed** — the previous shape stored English description strings in the array and ran them through a `describe()` switch that mapped each one back to a `__()` call. Adding a fifth shortcut without updating the switch would have silently fallen through to the untranslated string. The array now inlines `__()` directly at each entry; make-pot extracts them at the call site. **ToastHost wrapper aria-live removed** — `@wordpress/components` Snackbar already announces via its own aria-live region; the wrapper's `aria-live="polite"` would have caused some screen readers to double-announce. The wrapper now styles only.
-   `AssetLoader::enqueue()` now resolves the stylesheet basename. `@wordpress/scripts` (since v23) emits the CSS chunk for an entry `index` as `style-index.css` rather than `index.css`; the previous code looked for the legacy filename and would have silently skipped the new stylesheet. The loader now tries `style-index.css` first and falls back to `index.css` so a hand-built bundle following the older convention still enqueues. `AssetLoaderTest::test_enqueue_includes_style_when_css_present` updated to assert the new filename, and a new `test_enqueue_falls_back_to_legacy_index_css` covers the fallback branch.

## [0.8.0] - 2026-04-28

Closes Phase 8 of the [roadmap](ROADMAP.md): the Settings tab is now a real form. Admins can edit `log_path` and `tail_interval`, save through the existing `POST /settings` route, and probe a candidate log path through a new side-effect-free `POST /settings/test-path` endpoint that surfaces `PathGuard`'s verdict inline — including a clean fall-through to a parent-directory writability check so a fresh install pointing at a not-yet-created custom path gets a green answer instead of a misleading "does not exist" rejection. With the editor in place the Phase 7.4 tail loop now has a UI for changing its cadence, and the custom log path the rest of the codebase has been validating since Phase 3 finally has a way to be configured from wp-admin. Phase 11 (polish, a11y, i18n) is the next milestone before the v1.0.0-rc.1 cut.

### Added

-   `SettingsPanel` component (Phase 8.1) — the Settings tab in `App.jsx` now renders a real form built on `@wordpress/components` (`TextControl` × 2, `Button`, `Notice`, `Spinner`) backed by a new `settings` slice in the `logscope/core` store. The slice keeps `draft` and `values` separate so the admin can edit freely and Reset without mutating server-of-record state; Save POSTs the whole draft, the server returns the new authoritative shape, and the reducer copies it into both slots. The slice also carries `loadError` / `saveError` / `fieldErrors` / `testResult` / `testError` / `isTesting`, and the dispatcher gains `*fetchSettings` / `*saveSettings` / `*testLogPath` generator thunks consistent with the existing `*fetchLogs` shape. Per-field rejections from the existing `unknown_setting` 400 surface as inline notices under the offending input (mapping `error.data.unknown[]` into a `fieldErrors` map in the catch arm) rather than just a top-level banner. The dirty check trims `log_path` before comparing so a trailing space doesn't toggle Save into a dirty state for a no-op edit, matching what the server-side sanitiser would have stored anyway. Field-error wording is held in the panel layer (`translateFieldError(code)`) so the store records codes (`'unknown_setting'`) rather than untranslated strings — every user-facing string still goes through `__()`.
-   Custom log path UI + side-effect-free path probe (Phase 8.2). New `POST /wp-json/logscope/v1/settings/test-path` REST route on the existing `SettingsController`, gated by the same `permission_callback` as the rest of the surface (`logscope_manage` cap, 401 / 403 from the abstract base). The endpoint always returns 200 with a structured verdict body — a malformed candidate is a normal answer to a probe, not a request error — shaped `{ ok, resolved, exists, readable, writable, parent_writable, allowed_roots, reason }`. Successful resolution inside the allowlist sets `ok:true` with the canonical `resolved` path; a `MissingPathException` (new typed subclass of `InvalidPathException`, thrown by `PathGuard::resolve()` when `realpath()` returns `false`) falls back to `PathGuard::is_writable_parent_of()` so a fresh install pointing at a not-yet-created custom path gets `ok:true` with `exists:false` and `parent_writable:true` rather than a misleading "does not exist" rejection. Branching on a typed exception (rather than the message string) means a future PathGuard message edit cannot silently break the fallback. `writable` and `parent_writable` are kept distinct on the wire so the field name doesn't lie about which inode is writable. Rejected candidates carry the PathGuard message verbatim in `reason` (`../../../etc/passwd` → "Path contains a parent-directory segment.", a `/tmp/...` outside the allowlist → "Path is outside the allowed directories."), and the configured allowlist roots ride along on every verdict so the panel can name them in error copy.
-   Settings panel "Test path" button + verdict rendering. The button calls the new endpoint via `client.testLogPath(path)`; the inline result Notice shows the resolved absolute path on success, the rejection reason on failure, and the allowlist roots in a fenced `<code>` so the admin can see what's permitted. A transport failure (network down, 5xx) lands in a separate `testError` slot rendered as a neutral warning Notice ("Could not test the path: …"), keeping the previous successful verdict (if any) visible — so the admin can tell a probe failure from a path rejection. The button auto-clears the verdict on edit, and is disabled while a probe is in flight or when the field is empty.
-   DI + plumbing: `SettingsController` now takes `PathGuard` as a second constructor argument; `Plugin::register_services()` wires the existing canonical `path_guard` service into its factory, and the new route is registered inside the same `try/catch` group that already covered `/settings` so a misconfigured allowlist leaves both routes unregistered together rather than half-registering. `InvalidPathException` is no longer `final` so `MissingPathException` can extend it.
-   Tests (Integration / `SettingsControllerTest`): existing-file inside allowlist returns `ok:true` with `resolved`/`exists:true`/`readable:true`; nonexistent-file with writable allowlisted parent returns `ok:true` with `exists:false` / `writable:false` / `parent_writable:true`; nonexistent-file whose parent is outside the allowlist returns `ok:false` (regression guard for the typed-exception fallback); `../../../etc/passwd` returns `ok:false` with a non-empty `reason` (the Phase 8.2 AC); a path outside the allowlist returns `ok:false` with a reason mentioning "outside"; an empty/whitespace path returns `ok:false` with `Path is empty.`; and `register_routes()` is asserted to register both `/settings` (GET + POST) and `/settings/test-path` (POST with a required `path` arg). Existing test sandbox cleanup drops `@unlink`/`@rmdir` suppression so a leaking handle in CI surfaces.

## [0.7.0] - 2026-04-27

Closes Phase 7 of the [roadmap](ROADMAP.md): filters, grouping, stack-trace expansion, and tail mode. The Logs tab now ships a filter bar (severity multi-select, debounced regex, date range, source dropdown) URL-mirrored via `history.replaceState`, a list ↔ grouped view toggle backed by signature grouping, click-to-copy frame paths in expanded fatals, and a tail-mode toggle that polls `GET /logs?since=<last_byte>` on the admin-configured cadence with rotation detection and an "N new entries" pill when the user has scrolled away. Backend additions: `LogQuery::$since_byte`, `PagedResult::$last_byte`/`$rotated`, `LogsController` now serialises `frames` (gated to fatal/parse severities) and surfaces `last_byte`/`rotated` on every response. Settings UI lands in Phase 8.

### Changed

-   Phase 7.3 / 7.4 review follow-ups. **Tail rotation** — `LogRepository::query()` now treats `since_byte > $last_byte` as "the file shrunk between polls" (rotation or manual clear) rather than a no-op delta. The repository falls through to the normal full-read path and `PagedResult::$rotated` (new) propagates through the REST envelope as `rotated: true`; the React store's `TAIL_APPEND_ENTRIES` reducer treats a rotated response as a baseline replacement (clears the list, drops `newCount`, prunes `expandedTraces`) instead of prepending stale-cursor zero-length deltas. Without this, a rotated log would freeze the tail UI at the rotation point until the user manually refreshed. **Filter race in tail polling** — `useTailPolling` snapshots `filtersRef.current` at request send-time and discards the response if the live shape no longer matches, so a filter change mid-flight cannot land entries that don't satisfy the active filter set. **Frame-parse cost** — `LogsController::shape_entry()` now gates `StackTraceParser::parse()` on `severity === 'fatal' || 'parse'`; warnings, notices, deprecation rows never carry traces in WP's debug-log format, and gating cuts the per-row regex sweep on the typical noisy log by ~50× without changing the wire shape. **`entryKey` collisions** — the row identifier no longer short-circuits on `entry.raw` alone (PHP error logs routinely repeat identical lines, and a `raw`-only key would expand all duplicates when one was clicked); it now composes `timestamp|file|line|severity|message|raw` and lives in `assets/src/utils/entryKey.js` so the row component and the store's expand-prune logic share one definition. **Unbounded `expandedTraces` growth** — the `LOGS_RECEIVED` reducer now prunes the map to keys present in the new payload, so a long admin session with many filter changes can't bloat memory with dead toggle keys. **Grouped-while-tailing** — flipping `viewMode` to `grouped` while tail is active now auto-stops the polling loop in the `SET_VIEW_MODE` reducer (the symmetric case to "tail-on flips to list"), so the loop can no longer keep mutating state against a list the user can't see. **`buildFilterParams`** — the filter→query-string translation lives in `assets/src/utils/filterParams.js` and is shared between `LogViewer`'s primary fetch and `useTailPolling`'s loop, so the two cannot drift in their encoding. **Action arg rename** — `appendTailEntries`'s third argument is `atTop` (matched to what the viewer actually computes); the previous `scrolledToBottom` name read backwards because the viewer renders newest-first. The bogus `total: items.length` overwrite on tail append (which clobbered the server-reported total) is removed. New PHPUnit coverage for the tail-mode backend lives in `LogQueryTest` (since_byte 0/null/negative) and `LogRepositoryTest` (since_byte 0/mid/=size/>size, last_byte propagation across list and grouped paths) plus `LogsControllerTest` cases for `frames` shape on a fatal-with-trace fixture, the Notice gate that suppresses frame parsing, the `since` round-trip, and the rotation flag on the wire — closing the test gap the review flagged.
-   `LogQuery::parse_bound()` now uses the `Y-m-d|` reset format so a date-only bound parses to midnight rather than inheriting `DateTimeImmutable::createFromFormat()`'s default of "current time of day", which made `test_date_range_filter_excludes_outside_window` flaky after noon UTC.

### Added

-   `StackTracePanel` component (Phase 7.3) — expandable panel that lists each parsed frame with its `file:line` rendered as a click-to-copy button (per the AC, the clipboard payload is the exact `path/to/file.php:123` string). Frames lacking a file (`{main}`, `[internal function]`) drop the click target since there's nothing useful to copy. Per-row expansion state lives in the `logscope/core` store as an `expandedTraces` map keyed by the entry's `raw` line, because react-window recycles row components on scroll and a row-local `useState` would lose the toggle the moment the entry passed out of the viewport. To accommodate the inline panel, `LogViewer` switches the virtualized list to a function-form `rowHeight` that closes over the `expandedTraces` map — collapsed rows stay 48px and expanded rows grow to fit their frames, and the closure changing on every toggle is what tells react-window to re-measure. `LogsController::shape_entry()` now serialises a `frames` field via `StackTraceParser::parse( $entry->raw )` so the React side has the parsed structure without re-parsing client-side; parsing happens lazily on serialise rather than at parse time, since most entries are warnings/notices that have no trace and the regex sweep would be wasted work on rows the UI will never expand. `EntryRow` now keys on the lowercase `Severity::*` tokens via the shared `severityTone`/`severityLabel` util landed in 7.1 and renders a `▾ / ⋯` toggle in the row head; the row's outer `style` from react-window still positions and sizes the row, while the trace panel renders inside.
-   Tail mode (Phase 7.4) — toolbar toggle that polls `GET /logs?since=<last_byte>` on the admin-configured `tail_interval` and prepends new entries to the current list. The polling loop is a `setTimeout` chain (rather than `setInterval`) so a slow REST call cannot pile up overlapping requests; each tick waits for the previous response before scheduling the next, which means the practical floor on a slow host is the round-trip time, not the configured interval. Auto-scroll-to-top fires when the user is within 8px of the top of the list (the viewer renders newest-first, so "scroll to where new entries land" is the top edge); when the user has scrolled away, an `aria-live="polite"` "N new entries" pill appears, and clicking it both scrolls to the top and clears the counter. Filters are honoured during tail polling — only entries that match the current severity / regex / source filter are appended — so a user watching for fatals during a deploy doesn't drown in unrelated notices. Toggling tail on while in grouped mode flips the view to list, since the `since` byte-offset endpoint returns raw entries and grouped doesn't apply. New backend surface: `LogQuery::$since_byte` (validated `>= 0`), `LogRepository::query()` honours it by reading only `[since_byte, EOF)`, skipping pagination and returning every matching entry in chronological order, and `PagedResult` carries a new `last_byte` field equal to the source size at read time so the next poll has a starting point. Even non-tail responses now expose `last_byte`, so flipping tail on after a regular fetch resumes from the right offset without an extra round-trip. `since` past EOF is a no-op (a rotated/cleared log between polls returns nothing rather than erroring), and a value ahead of the file the server can see is treated as "nothing new since you last asked." `AssetLoader::localized_payload()` now ships `tailInterval` from `Settings::get('tail_interval')` so the React loop doesn't need a settings round-trip on page load. New `useTailPolling` hook owns the loop; the LogViewer handles the scroll-to-top affordance via the `useListRef` imperative element. Existing `AssetLoader` tests were updated to construct the loader with a real `Settings` instance and a stubbed `get_option` (Settings is final, so a Mockery double can't replace it).

-   `FilterBar` component (Phase 7.1) — severity multi-select (checkbox group keyed on the lowercase `Severity::*` tokens the REST layer ships, so the wire format and the UI control share one vocabulary), `from`/`to` date inputs, a regex search debounced 300ms, and a source dropdown populated from distinct file paths in the currently-loaded items. Filter state lives in the `logscope/core` store; a new `useUrlQuerySync` hook mirrors `view`, `severity`, `from`, `to`, `q`, and `source` into the URL via `history.replaceState` (so the back stack isn't littered with one entry per keystroke) and seeds initial state from the URL on mount, satisfying the AC that any filter change updates the URL + refetches. The regex input owns its own ephemeral state and lands in the store only after the debounce, so a fast typist doesn't fire one REST query per character. The source dropdown derives in-memory from `items[].file` rather than calling a separate `distinct_sources` REST route — the data is already in the response and a per-keystroke roundtrip would be wasteful; the trade-off is that the dropdown reflects the current page's sources, not the entire log.
-   `GroupedView` component (Phase 7.2) and a list/grouped mode toggle in the Logs tab. The toggle is wired to `viewMode` in the store, which the fetch effect translates into `?grouped=true` on the existing `GET /logs` route — no new endpoint. Toggling preserves filters (they're a separate slice) and restores per-mode scroll position: react-window v2's `<List>` exposes its scroll container via the imperative `element` field on `useListRef`, so the LogViewer captures `scrollTop` on each scroll event and writes it back on remount; the grouped pane uses a plain ref on its overflow container for the same effect. Group rows render severity pill, occurrence count, file:line, sample message, and an expandable detail pane (first/last seen, location, full sample). Groups are non-virtualized: the server caps a page at 500 rows (and ships 50 by default), DOM size is bounded, and the variable-height expanded panel would otherwise demand a measuring virtualizer. The expanded view shows the group's representative sample rather than every matching entry — surfacing every entry in a signature requires server-side filtering by signature hash, which is a Phase 8 / post-1.0 backend gap; flagging here so the limitation is captured.
-   `assets/src/utils/severity.js` — single source of truth for the `Severity::*` token → CSS-tone and translated-label mapping, used by `EntryRow`, `GroupedView`, and the `FilterBar` checkbox legend. Replaces the inline map in `EntryRow` that keyed on the human strings (`'Fatal error'`, `'Warning'`) and consequently always missed (the REST layer serialises lowercase tokens), so every row had previously rendered with the unknown-tone styling. `useDebouncedValue` and `useUrlQuerySync` hooks land alongside as small, single-purpose helpers.

## [0.6.0] - 2026-04-27

Closes Phase 6 of the [roadmap](ROADMAP.md): the admin page + React viewer shell. Logscope is now a visible feature in wp-admin — **Tools → Logscope** renders a React app gated by `logscope_manage`, served by a screen-scoped asset pipeline and a `@wordpress/data` store consuming the Phase-4/5 REST surface. Filters, grouping, trace expansion, and tail mode arrive in Phase 7; the settings UI lands in Phase 8.

### Added

-   `LogViewer`, `EntryRow`, `EmptyState` React components — the Logs tab now renders a virtualized list of debug-log entries fetched from `GET /logs`. Virtualization is `react-window` v2's `<List>` (added as a dep — ~6 KB gz, far less risk than a hand-rolled virtualizer); the rendered DOM is bounded by viewport height regardless of result-set size. Row height is fixed at 48px for the Phase 6 shell — variable heights would push trace expansion into a second virtualizer and double the per-row measure cost; we'll revisit if 7.3 reveals a UX problem with the truncated message. `EntryRow` renders a severity pill (mapped to fatal/warning/notice/deprecated/unknown tones), the WP timestamp, the truncated message, and a small `⋯` indicator on entries that carry a stack trace; the actual trace expansion is intentionally deferred to `StackTracePanel` in step 7.3 because per-row React state is lost when react-window recycles the row component on scroll, so the right home for the expanded/collapsed map is a parent-owned dictionary keyed by signature. `EmptyState` distinguishes a fresh-install zero-results screen from a fetch-failure error screen, so an admin sees a different cue when the network blew up versus when the log is genuinely empty. `LogViewer` dispatches `fetchLogs` on mount and renders a `Spinner` only on the first load (subsequent refreshes show the existing list with `aria-busy=true` so the user isn't punished for refetching). Bundle weight grew from 3.26 KiB to 14 KiB on this step (`react-window` + the three new components). The 5k-entry smoke test the AC calls for has not been run yet — it requires a live WP install to exercise; deferring to the human-eyes pass before the v0.6.0 tag in 6.5.
-   React app shell (`assets/src/index.js`, `components/App.jsx`, `store/index.js`, `api/client.js`). Entry mounts on `#logscope-root` (the contract `PageRenderer::ROOT_ELEMENT_ID` mirrors), gated on `DOMContentLoaded` so a script ordering quirk on the host page can't race the mount. The `@wordpress/data` store is registered as `logscope/core` with a minimal Phase-6 shape (active tab + a logs slot the viewer in 6.4 populates); the `fetchLogs` generator yields each action (start → receive | fail) rather than returning the terminal action, since `@wordpress/redux-routine` only dispatches yielded values — a returned action is the generator's return value, not a dispatch, and would leave the store stuck in `isLoading: true`. The REST client wraps `@wordpress/api-fetch` with `createRootURLMiddleware` (REST root) + `createNonceMiddleware` (the `wp_rest` nonce), both read from `window.LogscopeAdmin` that `AssetLoader` localizes — every Logscope call goes through one client so the nonce + namespace contract has a single owner. Middleware registration is guarded by a flag stashed on `apiFetch` itself so an HMR re-import or a duplicate import path can't stack the same middleware twice (which would prepend the REST root to an already-prefixed URL). The shell renders a two-tab layout (Logs · Settings) controlled by the store rather than `<TabPanel initialTabName>` (the latter only seeds once and would not repaint on a back-button hash navigation); a single hashchange listener turns every hash mutation into one store dispatch, and tab clicks set `location.hash` so the listener is the single source of truth — eliminating the double-dispatch the earlier draft had. Tab bodies render the LogViewer (Logs) or a placeholder pointing at Phase 8 (Settings).
-   `Logscope\Admin\AssetLoader` — enqueues the React bundle (`assets/build/index.js` + optional `index.css`) **only on the Logscope screen**, comparing the `admin_enqueue_scripts` hook suffix against the value `Menu::register()` captured at registration time so unrelated wp-admin pages stay byte-for-byte identical (a recurring wp.org reviewer flag for plugins that enqueue everywhere). Dependencies and version hash come from the `index.asset.php` file `@wordpress/scripts` writes on every successful build, so adding a `@wordpress/components` import to a React file automatically wires `wp-components` into the enqueue without hand-maintaining a dep list. A missing asset file is treated as "build hasn't run / broken zip" and silently skips enqueue; the renderer's empty `#logscope-root` then serves as the visible failure mode. CSS is conditionally enqueued only when `index.css` exists, since pure-JS builds don't emit a stylesheet. `wp_localize_script` writes a `window.LogscopeAdmin` bootstrap object carrying `restUrl` (Logscope namespace), `restRoot` (for `@wordpress/api-fetch`), `nonce`, and `canManage`; `wp_set_script_translations` wires up the `logscope` text domain. `Plugin` registers `admin.asset_loader` and hooks `enqueue_admin_assets` on `admin_enqueue_scripts` inside the same `Throwable` trap as the other lifecycle callbacks. Unit tests cover the screen-id no-op, the missing-asset-file no-op, the script enqueue with deps + version, the conditional CSS enqueue, and the localized payload shape.
-   `Logscope\Admin\Menu` and `Logscope\Admin\PageRenderer` — register the **Tools → Logscope** submenu (slug `logscope`) gated by the `logscope_manage` capability (resolved through `Capabilities::required()` so the `logscope/required_capability` filter still applies). The renderer outputs the wp-admin `<div class="wrap">` shell with a translated `<h1>Logscope</h1>` and a single mount node (`<div id="logscope-root">`) the React app will hydrate in step 6.3 — kept intentionally bare so a JS bundle failure surfaces as a visibly empty page rather than a half-rendered hybrid. `PageRenderer::ROOT_ELEMENT_ID` is the canonical contract between PHP and the React entry. `Plugin` registers `admin.page_renderer` + `admin.menu` services and wires `register_admin_menu()` on `admin_menu`, wrapped in the same `Throwable` trap as `register_rest_routes()` so a DI-graph failure routes through the `WP_DEBUG`-gated breadcrumb instead of aborting menu registration for unrelated plugins. Unit tests cover the `add_submenu_page` invocation shape, the filter override path, the `false` return defensiveness, and the renderer markup contract.

## [0.5.0] - 2026-04-27

Closes Phases 4 and 5 of the [roadmap](ROADMAP.md): the REST surface React will consume in Phase 6 (`GET/DELETE /logs`, `GET /logs/download`, `GET/POST /settings`) plus the schema-driven settings backend. The `RestController` abstract base centralises capability + authentication enforcement so every Logscope route inherits a uniform 401/403 contract; the `Plugin` DI graph wires `path_guard` → `log_source` → `log_repository` → controllers and registers logs and settings routes in independent `try/catch` blocks so a misconfiguration in one cannot abort `rest_api_init` for the other or for unrelated plugins. Still no user-visible features — the React admin page lands in Phase 6.

### Changed

-   Phase 5 review follow-ups. `Plugin::register_rest_routes()` no longer silently swallows DI-graph failures: each `try/catch` now routes through a private `log_route_registration_failure()` helper that writes a one-line breadcrumb (route group, exception class, message) via `error_log()` when `WP_DEBUG` is on, so an admin reporting a 404 on `/logs` or `/settings` has something to grep for. `SettingsController::handle_post()` now applies writes in two phases — sanitise the entire body into a temp array first, then persist — so a sanitiser that throws on a later key cannot leave earlier keys partially saved in `wp_options`. The previously-dead `logscope_rest_invalid_setting` catch is now reachable for any future schema-side validation. `SettingsSchema` builds its field map eagerly in the constructor (the lazy guard bought nothing, since closures were already `static` and the data is constant) and exposes a `TYPES` const that closes the type vocabulary; `matches_type()` no longer needs a defensive default branch. The `tail_interval` sanitiser now uses `is_numeric()` + `(int)` so `'3.5'` truncates to `3` instead of falling back to the default `1`, matching what an admin would expect from a number input. `SettingsSchema` is no longer `final` so a test-only subclass can inject a sanitiser that throws — used by a new atomicity test that proves a phase-1 failure on `tail_interval` does not leak a phase-2 write of `log_path`.
-   Phase 4 review follow-ups. Removed the unused `RestController::register()` indirection — `Plugin::register_rest_routes()` is now the single registration point for Logscope routes, eliminating a latent double-registration hazard. `Plugin::register_rest_routes()` now traps `Throwable` (was `InvalidPathException` only) so any constructor-time failure in the `path_guard` → `log_source` → `log_repository` → `rest.logs_controller` graph leaves the routes unregistered without aborting `rest_api_init` for other plugins. `LogsController::normalise_severity_param()` now drops unknown tokens against `Severity::all()` for the comma-separated string path, matching the args-schema enum constraint that already covered the array form. `LogsController::download_headers_for()` strips control chars, double-quotes, and backslashes from the basename before interpolating into `Content-Disposition`, falling back to `debug.log` if everything is stripped, so a sibling renamed externally cannot produce a malformed header. The `_logscope_skip_exit_for_tests` request-param escape hatch now carries an explicit `@internal` docblock noting it must be removed if the cap gate ever weakens.

### Added

-   `Logscope\REST\SettingsController` — `GET` + `POST /wp-json/logscope/v1/settings`. `GET` returns the full settings shape produced by `Settings::all()`; `POST` accepts a partial body, rejects bodies that contain any key the schema does not know with a 400 `logscope_rest_unknown_setting` (the error data carries the offending keys under `unknown` so the React client can highlight the right field), then sanitises and persists each known key through `Settings::set()` and returns the new full shape so the client can update its store without a follow-up GET. The unknown-key rejection is all-or-nothing: a request mixing one valid and one invalid key is refused outright rather than partially saved, so a typo cannot leave the settings in a half-applied state. `InvalidArgumentException` from the schema is caught and translated to a 400 `logscope_rest_invalid_setting` as a belt-and-suspenders fallback. `Plugin` registers `rest.settings_controller` in the DI container, and `register_rest_routes()` now wires logs and settings routes in independent `try/catch` blocks so a failure in one DI subgraph cannot take down the other (or abort `rest_api_init` for unrelated plugins). Integration tests (with a `wp_options`-backed `get_option`/`update_option` stub) cover the GET shape, POST persisting + returning new state with sanitisation, the AC `tail_interval=0` → `1` coercion, the unknown-key 400 (with `unknown` payload + no partial save), the empty-body no-op, and the `register_rest_route()` invocation shape.
-   `Logscope\Settings\SettingsSchema` and `Logscope\Settings\Settings` — schema-driven settings facade backing the v1.0 surface (`log_path`, `tail_interval`). The schema is the single source of truth for each field's underlying `wp_options` row name, scalar type, default, and sanitizer; a sanitizer always runs on `set()` so the REST layer can pass raw input through without pre-cleaning. `tail_interval` coerces non-int input and values `< 1` to `1` (matching the roadmap "min 1s"); `log_path` strips null bytes and trims whitespace, leaving filesystem-semantic validation to `PathGuard` on use. `Settings::get()` re-validates the stored value against the declared type and falls back to the schema default when an older version of the plugin left a corrupted row, so callers never see the wrong shape. `Settings::all()` exposes the full settings view that the REST controller will return on `GET /settings`. `Settings::set()` and the schema both throw `InvalidArgumentException` for unknown keys; the REST boundary translates that into a 400. `Plugin` registers `settings.schema` and `settings` services in the DI container, and `Activator::DEFAULT_OPTIONS` carries a docblock cross-link so the activation seed and the runtime schema cannot drift unnoticed. Unit tests cover the schema (defaults, sanitizer happy paths, `tail_interval=0` → `1`, type matchers, unknown-key rejection) and the facade (Brain-Monkey-mocked `get_option`/`update_option` round-trip, wrong-type fallback, set-strips-null-bytes, `all()` shape).
-   `Logscope\REST\RestController` — abstract base for every Logscope REST controller. Centralises the `logscope/v1` namespace, `register_routes()` registration on `rest_api_init`, a uniform `permission_callback()` returning a 401 `WP_Error` for unauthenticated callers and a 403 for users missing the Logscope manage capability, and an `error()` factory that maps internal failures to `WP_Error` shapes the REST dispatcher will turn into the right HTTP status. Cookie-nonce verification stays delegated to core's `rest_authentication_errors` pipeline so application-password and other auth schemes plug in without bespoke handling. Unit tests cover the 401/403/allowed branches, the `rest_api_init` registration, the `error()` payload shape, and the namespace constant; a slim `WP_Error` stub plus a stub subclass make the base testable without a full WordPress harness.
-   `LogsController` — `DELETE /wp-json/logscope/v1/logs` and `GET /wp-json/logscope/v1/logs/download`. Clear is a soft-delete that renames the active log to `<basename>.cleared-YYYYMMDD-HHMMSS` (UTC) so a wiped log is still recoverable for post-mortems; the destructive verb requires `?confirm=true` to fire (returns a 400 `logscope_rest_confirmation_required` otherwise) and 404s when no log file exists yet. Before renaming, the controller asks `PathGuard::is_writable_parent_of()` to confirm the directory is writable, returning a 403 `logscope_rest_log_not_writable` when it isn't, so a misconfigured `chown` surfaces as a clean error rather than a silent failure. The download route streams the raw log with `Content-Type: text/plain`, `Content-Disposition: attachment`, `X-Content-Type-Options: nosniff`, an explicit `Content-Length`, and `Cache-Control: no-store` headers, then `readfile()` + `exit` to bypass core's REST JSON serialiser; a `_logscope_skip_exit_for_tests` request param skips header-emit and `exit` so the unit suite can drive the streaming path under PHPUnit's already-flushed output buffer. `LogsController::download_headers_for()` and `archive_path_for()` are exposed as static pure helpers so the wire contract is asserted directly without filesystem or networking. New tests cover: clear-without-confirm 400, clear-on-missing-log 404, clear renames file with the expected `cleared-YYYYMMDD-HHMMSS` pattern, archive-path helper, download-on-missing-log 404, download header shape, and end-to-end stream-to-output-buffer.
-   `Logscope\Support\PathGuard::is_writable_parent_of()` — companion to `is_writable()` that resolves the parent directory of a candidate path through the existing allowlist and returns `true` only when the parent is canonical, allowlisted, and writable by the PHP process. Used by `LogsController::handle_clear` for the soft-delete pre-flight and earmarked for the Phase 8 "Test path" settings probe; the file itself need not exist yet, which matches both use cases. Unit tests cover the writable-parent happy path, outside-allowlist rejection, null-byte rejection, and empty-string rejection.
-   `Logscope\REST\LogsController` — `GET /wp-json/logscope/v1/logs` returning paginated, filtered log views. Args schema accepts `page`, `per_page` (1–500, default 50), `severity` (array of `Severity` enum values, also accepts a comma-separated string for clients that prefer flat query strings), `from` / `to`, `q` (regex, ≤200 chars), `grouped`, and `source`; core's REST validator rejects out-of-range or wrong-type params before our handler runs. Response body is `{items, total, page, per_page, total_pages}`, and pagination totals are mirrored into `X-WP-Total` / `X-WP-TotalPages` headers so the React client matches core's collection convention. `LogQueryException` (bad regex, unparseable date, etc.) is caught and translated to a 400 `WP_Error` rather than bubbling as a 500. `Plugin` now owns a small DI graph wiring `path_guard` → `log_source` → `log_repository` → `rest.logs_controller`, and registers the controller's routes on `rest_api_init`; a misconfigured custom log path causes `register_rest_routes()` to swallow the `InvalidPathException` so other plugins' route registration is not aborted. Tests stub `WP_REST_Request` / `WP_REST_Response` (via test bootstrap) and exercise the handler end-to-end against a real fixture log: the AC scenario (page 2 of 50 with `severity=Fatal` over 75 fatals) plus grouped output, comma-separated severity, missing-log empty set, the 400-on-bad-regex path, and the `register_rest_route()` invocation shape.

## [0.4.0] - 2026-04-27

Closes Phase 3 of the [roadmap](ROADMAP.md): the log-reading and parsing foundation. Logscope can now safely locate a `debug.log` (PathGuard allowlist + traversal protection), stream bytes out of it without loading the whole file into memory, parse the WordPress severity formats and PHP stack traces, group "the same error happening over and over" by signature, and serve filtered, paginated views through a single repository facade. Still no user-visible features — REST routes and the React UI come next in Phase 4 onward.

### Added

-   `Logscope\Support\PathGuard` — filesystem path validator with two-stage rejection (raw-string checks for null bytes and `..` segments before any filesystem call, then `realpath()` canonicalisation with allowlist containment). Symlink escapes are caught implicitly because `realpath()` resolves links before the containment check; sibling-prefix attacks (e.g. `/var/www-evil` against root `/var/www`) are rejected by the exact-separator suffix match. Constructor fails closed: a non-existent root is silently dropped so a misconfigured root cannot widen the allowlist. Companion `InvalidPathException` carries plain-English internal messages; translation happens at the REST/UI boundary later.
-   `Logscope\Log\LogSourceInterface` and `FileLogSource` — byte-level abstraction over the underlying log file with `path() / exists() / size() / read_chunk(from, max)`. Reads use `fopen('rb') / fseek / fread` so multi-hundred-MB logs never hit memory all at once. Constructor validates the parent directory through `PathGuard` and reassembles the final path from the validated dirname plus `basename()` of the input — keeping the contract usable on a fresh WordPress install where `debug.log` does not yet exist, while still defeating traversal in the input. Past-EOF, missing-file, and non-positive-bound reads return an empty string rather than throwing.
-   `Logscope\Log\LogParser`, `Entry`, and `Severity` — pure parser turning raw debug-log text into structured `Entry[]`. Recognises all six WordPress severity tokens (`Fatal error`, `Parse error`, `Warning`, `Notice`, `Deprecated`, `Strict Standards`) plus an `unknown` bucket for unsevered `error_log()` output. Parses WP's `[DD-Mon-YYYY HH:MM:SS [TZ]]` timestamp form with or without the timezone token. Continuation lines (stack-trace rows, `thrown in ...` tails, blank separators) attach to the previous entry's `raw` field; orphan continuations at chunk start are dropped because chunk stitching is the future repository's responsibility. File and line are extracted from both `... in /path.php:N` and `... in /path.php on line N` shapes.
-   `Logscope\Log\StackTraceParser` and `Frame` — parses PHP's `#N <file>(<line>): <call>` stack-trace rows into `Frame[]`. Recognises bare-function, instance (`->`), and static (`::`) calls with namespaced class names; special `[internal function]` and `{main}` frames are represented by null patterns rather than dedicated subtypes. The file regex anchors on the rightmost `(\d+):` so Windows-style paths like `C:\Program Files (x86)\app\main.php` parse correctly even with parens earlier in the path. Argument lists are captured as raw strings and never evaluated, since PHP's trace output for arguments routinely contains truncated literals and `Object(...)` placeholders that are not valid source.
-   `Logscope\Log\LogGrouper` and `Group` — collapses `Entry[]` into "the same error happening over and over" buckets. The signature is `md5(severity | file | line | normalised-message)`, where normalisation strips hex addresses (`0x[0-9a-fA-F]+` → `0xN`), single- and double-quoted strings (→ `'?'` / `"?"`), and bare integers (→ `N`). Order matters: hex first so the digit rule does not eat address bytes. Groups carry a running `count`, the first-observed `sample_message` for display, and a `first_seen` / `last_seen` window built from parsed `DateTimeImmutable` comparisons rather than raw string compares (the WP timestamp format is not lexically sortable across months). Output is sorted by descending count, with ties broken by most-recent `last_seen` then signature for stable ordering across runs.
-   `Logscope\Log\LogRepository`, `LogQuery`, `PagedResult`, `SourceClassifier`, and `LogQueryException` — facade over the source / parser / grouper stack with paginated, filtered access. `LogQuery` validates severity, date window (`Y-m-d` or `Y-m-d H:i:s`), regex (≤ 200 chars, server-side, message-scoped), source slug (`plugins/<slug>` / `themes/<slug>` / `mu-plugins/<slug>` / `core` produced by `SourceClassifier`), grouped flag, and pagination at construction; downstream code can trust every field. `LogRepository::query()` returns a `PagedResult` with `items`, `total`, `page`, `per_page`, `total_pages` ready for `X-WP-Total` / `X-WP-TotalPages` headers. Ungrouped queries return entries newest-first (reverse of file order, since WP appends); grouped queries return `Group[]` in count-desc order. Whole-file reads are bounded by `MAX_BYTES_PER_QUERY = 50 MB` — pathological logs are tail-clipped and the parser drops the orphan continuation at the cutoff. `distinct_sources()` exposes the dropdown population list. New `Integration` PHPUnit suite registered; `tests/php/Integration/Log/LogRepositoryTest.php` covers the AC end-to-end (page 2 of 50 with `severity=Fatal` over a 75-fatal fixture).

### Security

-   PathGuard is the foundation for all log-file access in later phases — every `FileLogSource` and settings-side path test will route through it before touching disk.

## [0.3.0] - 2026-04-23

Closes Phase 2 of the [roadmap](ROADMAP.md): the plugin now has a bootstrap entry point, a lightweight DI container, activation/deactivation/uninstall lifecycle, a reusable capability helper, and the PHPUnit + Brain Monkey scaffolding future tests will build on. Still no user-visible features.

### Added

-   Plugin bootstrap: `src/Plugin.php` with a hand-rolled lazy service container (`register`/`has`/`get`) and a single `Plugin::boot()` entry point. `logscope.php` now defines `LOGSCOPE_PLUGIN_FILE`, requires Composer autoload, and hooks `Plugin::boot` on `plugins_loaded` priority 5. Fires the `logscope/booted` action once the container is built so extensions can register services. Text domain is loaded on `init`.
-   Lifecycle hooks: `Activator` seeds default options (`logscope_log_path`, `logscope_tail_interval`, `logscope_db_version`) and grants the `logscope_manage` capability to administrators. `Deactivator` clears Logscope-owned cron events. `uninstall.php` deletes all `logscope_*` options, transient prefixes, and removes `logscope_manage` from every role — works whether the plugin is active at the time of deletion or not.
-   Unit test scaffolding: `brain/monkey` dev dependency, `phpunit.xml.dist`, `tests/php/bootstrap.php`, and a shared `Logscope\Tests\TestCase` base class wiring Brain Monkey setup/teardown.
-   `Logscope\Support\Capabilities` helper with `required()` and `has_manage_cap(?int $user_id = null)`. The `logscope/required_capability` filter lets site owners remap the required cap; non-string / empty filter returns fall back to the default so authorization cannot be silently disabled.

## [0.2.0] - 2026-04-22

Closes Phase 1 of the [roadmap](ROADMAP.md): composer, pnpm, phpcs, ESLint/Prettier, CI, and pre-commit hooks are all in place. No user-visible plugin behavior yet.

### Added

-   Composer tooling: `composer.json` with PSR-4 autoload (`Logscope\` → `src/`), WordPress Coding Standards (phpcs + wpcs + PHPCompatibilityWP) and PHPUnit ^9 as dev dependencies, and `lint` / `lint:fix` / `test` scripts. Platform pinned to PHP 8.0 to match plugin minimum.
-   PHPCS ruleset `phpcs.xml.dist`: WordPress-Core / -Docs / -Extra + PHPCompatibilityWP, `testVersion` 8.0-, `minimum_supported_wp_version` 6.2, text domain `logscope`. Excludes `vendor/`, `node_modules/`, `assets/build/`, `languages/`. `WordPress.Files.FileName` sniff disabled to allow PSR-4 file naming.
-   JS tooling: `package.json` with `@wordpress/scripts` build pipeline, `@wordpress/components` / `data` / `i18n` / `api-fetch`, React 18, and `@wordpress/eslint-plugin` + `@wordpress/prettier-config`. Scripts: `build`, `start`, `lint:js`, `format`, `packages-update`. Flat ESLint config via `eslint.config.mjs`, Prettier via `.prettierrc` + `.prettierignore`, `.npmrc` with `strict-peer-dependencies=false`. Entry point placeholder at `assets/src/index.js`; build output path `assets/build/` (gitignored). pnpm pinned via `packageManager`, Node ≥20.17 (required by `lint-staged` v16; Node 18 is EOL as of April 2025).
-   GitHub Actions CI workflow `.github/workflows/ci.yml` with four jobs: `php-lint` (matrix PHP 8.0–8.4), `js-lint` (`pnpm lint:js` + `pnpm build`), `audit` (`composer audit` + `pnpm audit --audit-level=high --prod`), and `plugin-check` (wp.org [Plugin Check](https://github.com/WordPress/plugin-check-action), informational until Phase 12.1). Triggers: push to `main`, all pull requests, manual dispatch. Concurrency group cancels in-progress runs on the same ref.
-   Pre-commit hook via [husky](https://typicode.github.io/husky) + [lint-staged](https://github.com/lint-staged/lint-staged): auto-runs `vendor/bin/phpcbf` on staged `*.php` files and `prettier --write --ignore-unknown` on staged `*.{js,jsx,json,md,css,yml,yaml}` files. Installed on `pnpm install` via the `prepare` script. `.husky/` and `.npmrc` added to `.gitattributes export-ignore` so they stay out of the wp.org zip.

## [0.1.0] - 2026-04-20

### Added

-   Initial project scaffold: folder structure matching the target architecture, plugin header file, GPL v2 license, AI agent rules (`AGENTS.md`, `CLAUDE.md`), EditorConfig, `.gitignore`, and `.gitattributes` with wp.org release-export hygiene.
-   No runtime behavior yet — plugin activates cleanly in WordPress 6.2+ on PHP 8.0+ and does nothing.

[Unreleased]: https://github.com/waqarahmadweb/logscope/compare/v0.14.0...HEAD
[0.14.0]: https://github.com/waqarahmadweb/logscope/compare/v0.13.0...v0.14.0
[0.13.0]: https://github.com/waqarahmadweb/logscope/compare/v0.12.0...v0.13.0
[0.12.0]: https://github.com/waqarahmadweb/logscope/compare/v0.11.0...v0.12.0
[0.11.0]: https://github.com/waqarahmadweb/logscope/compare/v0.10.0...v0.11.0
[0.10.0]: https://github.com/waqarahmadweb/logscope/compare/v0.9.0...v0.10.0
[0.9.0]: https://github.com/waqarahmadweb/logscope/compare/v0.8.0...v0.9.0
[0.8.0]: https://github.com/waqarahmadweb/logscope/compare/v0.7.0...v0.8.0
[0.7.0]: https://github.com/waqarahmadweb/logscope/compare/v0.6.0...v0.7.0
[0.6.0]: https://github.com/waqarahmadweb/logscope/compare/v0.5.0...v0.6.0
[0.5.0]: https://github.com/waqarahmadweb/logscope/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/waqarahmadweb/logscope/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/waqarahmadweb/logscope/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/waqarahmadweb/logscope/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/waqarahmadweb/logscope/releases/tag/v0.1.0
